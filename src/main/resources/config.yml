# =====================================================
# AutoFireWorkRegions 1.2
# Automatic Firework Show Plugin
#
# Плагин разработан командой Minecraft-проекта FlexCraft — Хэлфи ツ
#
# 🎮 IP сервера: mc-flexcraft.com (1.16.5 - 1.21.x)
# 🌐 Официальный сайт: https://www.flexdonate.com
# 💬 Discord-сообщество: https://discord.gg/KSdY9z4Fb9
#
# Описание:
# Плагин автоматически запускает красивые, масштабные и мощные
# фейерверки в заданных регионах (по координатам), создавая
# зрелищное шоу на спавне или в любой другой точке мира.
#
# Идеально подходит для:
# • Спавна
# • Ивентов
# • Праздников и особых событий
# =====================================================


# -----------------------------------------------------
# КАК ЧАСТО ЗАПУСКАЕТСЯ ШОУ (в секундах)
#
# interval-seconds: 1
# → означает, что каждые 1 секунду будет выполняться запуск
# фейерверков во всех зонах
#
# Рекомендуемые значения:
# 1  — интенсивное шоу (НЕ рекомендуется без лимитов)
# 2–3 — умеренное, красиво и без нагрузки
# 5+ — редкие праздничные залпы
# -----------------------------------------------------
interval-seconds: 1


# -----------------------------------------------------
# ЗАЩИТА ОТ ЛАГОВ (КРИТИЧЕСКИ ВАЖНО)
# -----------------------------------------------------

# Максимальное количество фейерверков,
# которые МОГУТ существовать одновременно в ОДНОЙ зоне
#
# Даже если burst и interval большие —
# больше этого лимита фейерверков не будет.
#
# Рекомендуется:
# 20–30  — безопасно
# 40–50  — зрелищно, но тяжелее
# 60+    — НЕ рекомендуется
max-fireworks-per-zone: 25


# Через сколько секунд фейерверк будет
# принудительно удалён (даже если не взорвался)
#
# Защищает от накопления entity и падения TPS
#
# Рекомендуется:
# 5–7 секунд — оптимально
firework-lifetime-seconds: 6


# -----------------------------------------------------
# ЗОНЫ ЗАПУСКА ФЕЙЕРВЕРКОВ
#
# Здесь указываются области, в которых плагин будет
# выбирать случайные точки для запуска фейерверков.
#
# ⚠ ВАЖНО:
# • Y-координата используется ТОЛЬКО как диапазон поиска
# • Реальный запуск ВСЕГДА происходит НАД землёй
# -----------------------------------------------------
zones:
  spawn:
    world: spawn
    min:
      x: -171
      y: -30
      z: -293
    max:
      x: 355
      y: 318
      z: 473


# -----------------------------------------------------
# НАСТРОЙКИ ФЕЙЕРВЕРКОВ
# -----------------------------------------------------
firework:

  # ---------------------------------------------------
  # СИЛА ФЕЙЕРВЕРКА (power)
  #
  # Чем выше значение — тем выше взлетает фейерверк
  # и тем больше радиус взрыва.
  #
  # Рекомендуемые значения:
  # 1–2 — маленькие
  # 3–4 — красивые и заметные (рекомендуется)
  # 5+  — очень мощные (может быть шумно)
  # ---------------------------------------------------
  power: 3


  # ---------------------------------------------------
  # КОЛИЧЕСТВО ФЕЙЕРВЕРКОВ В ОДНОЙ ТОЧКЕ (BURST)
  #
  # За один запуск в ОДНОЙ точке будет запущено
  # случайное количество фейерверков
  # от burst-min до burst-max.
  #
  # ⚠ ВАЖНО:
  # Это значение умножается на locations-per-interval
  # и ограничивается max-fireworks-per-zone
  # ---------------------------------------------------
  burst-min: 5
  burst-max: 15


  # ---------------------------------------------------
  # КОЛИЧЕСТВО ТОЧЕК ЗАПУСКА ЗА ОДИН ИНТЕРВАЛ
  #
  # locations-per-interval определяет,
  # СКОЛЬКО РАЗНЫХ МЕСТ будет выбрано за один запуск.
  #
  # В каждой точке запускается burst фейерверков.
  #
  # Пример:
  # interval-seconds: 1
  # locations-per-interval: 3
  # burst-min: 5
  # burst-max: 15
  #
  # → максимум 45 фейерверков,
  # НО фактически ограничено max-fireworks-per-zone
  # ---------------------------------------------------
  locations-per-interval: 3


  # ---------------------------------------------------
  # ЦВЕТА ФЕЙЕРВЕРКОВ
  #
  # Используются стандартные цвета Bukkit
  # (org.bukkit.Color)
  #
  # Чем больше цветов — тем разнообразнее шоу
  # ---------------------------------------------------
  colors:
    - RED
    - BLUE
    - AQUA
    - PURPLE
    - LIME
    - ORANGE
    - YELLOW
